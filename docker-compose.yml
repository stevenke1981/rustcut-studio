services:
  rustcut:
    build: .
    ports:
      - "8787:8787"
    environment:
      RUSTCUT_BIND: 0.0.0.0:8787
      RUSTCUT_DATA_DIR: /data
      RUST_LOG: rustcut=info,tower_http=info
    volumes:
      - ./data:/data
      - ./media:/media:ro
    restart: unless-stopped
